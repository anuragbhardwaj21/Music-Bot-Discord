first install discord library using:
	python3 -m pip install -U discord.py

then install youtube module using:
	pip install youtube_dl

add your bot to your discord after creating it.

after it copy the musicbot.py file and pass you token inside "client.run('Your_Token')"
